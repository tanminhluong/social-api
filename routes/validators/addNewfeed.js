const {check} = require('express-validator')

module.exports = [
    // check('content')
    // .exists().withMessage('Vui lòng cung nội dung')
    // .notEmpty().withMessage('Vui lòng không để trống nội dung'),
]